name: Release-Pipeline

# Trigger on push(merge) to main branch
on:
  push:
    branches:
      - main

jobs:
  call-release-pipeline:
    uses: terraform-ibm-modules/common-pipeline-assets/.github/workflows/common-release.yml@v1.17.6
    secrets: inherit
